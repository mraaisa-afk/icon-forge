[package]
name = "isg-core"
description = "Icon Forge core: provisional (spike-scoped) wasm-compatible types and pipeline traits"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# Hard constraint (keystone architecture decision): isg-core compiles for both
# native (linked into the Tauri binary) and wasm32-unknown-unknown (webview).
# CI enforces: cargo check --target wasm32-unknown-unknown -p isg-core

[dependencies]

[dev-dependencies]
